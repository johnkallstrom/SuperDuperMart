<nav class="navbar navbar-expand-lg bg-warning shadow-sm">
    <div class="container">
        <NavLink class="navbar-brand" href="/" Match="NavLinkMatch.All"><i class="bi bi-radioactive"></i> SuperDuperMart</NavLink>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navigation">
            <ul class="navbar-nav me-auto">
                <AuthorizeView Roles="@Roles.Customer">
                    <Authorized>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/products">Products</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/cart">Cart</NavLink>
                        </li>
                    </Authorized>
                </AuthorizeView>
                <AuthorizeView Roles="@Roles.Administrator">
                    <Authorized>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/manage/products">Products</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/manage/users">Users</NavLink>
                        </li>
                    </Authorized>
                </AuthorizeView>
            </ul>
            <ul class="navbar-nav ms-auto">
                <AuthorizeView>
                    <Authorized>
                        <li class="nav-item dropdown">
                            <NavLink class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                                <AuthorizeView Roles="@Roles.Customer" Context="Customer">
                                    <Authorized>
                                        <i class="bi bi-person-fill"></i>
                                    </Authorized>
                                </AuthorizeView>
                                <AuthorizeView Roles="@Roles.Administrator" Context="Administrator">
                                    <Authorized>
                                        <i class="bi bi-person-fill-gear"></i>
                                    </Authorized>
                                </AuthorizeView>
                            </NavLink>
                            <ul class="dropdown-menu">
                                <li>
                                    <h6 class="dropdown-header">@context.User.Identity?.Name</h6>
                                </li>
                                <li>
                                    @{
                                        string? email = context.User.FindEmail();
                                        if (!string.IsNullOrEmpty(email))
                                        {
                                            <small class="dropdown-item text-muted disabled">@email</small>
                                        }
                                    }
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li><NavLink class="dropdown-item" href="#" @onclick="Logout">Logout</NavLink></li>
                            </ul>
                        </li>
                    </Authorized>
                    <NotAuthorized>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="/login">Login</NavLink>
                        </li>
                    </NotAuthorized>
                </AuthorizeView>
            </ul>
        </div>
    </div>
</nav>